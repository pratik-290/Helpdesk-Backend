spring.application.name=helpdesk-pro-backend

spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:helpdesk}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Pratik@29}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=${PORT:8086}

app.jwt.secret=${JWT_SECRET:helpdeskprosecretkeyhelpdeskprosecretkey12345}

app.frontend.url=${FRONTEND_URL:http://localhost:5173}